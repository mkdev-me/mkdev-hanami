<div class="col-md-6 col-md-offset-3">
  <ul class="nav nav-pills nav-stacked">
    <% folders.each do |folder| %>
      <li>
        <%= link_to folder.name, routes.folder_path(id: folder.id) %>
      </li>
    <% end %>
  </ul>
</div>

